import React from 'react-native';

export const styles = React.StyleSheet.create({
  conversation: {
    // position: 'absolute',
    // bottom: 0,
    flex: 1,
    justifyContent: 'flex-end',
  },
});
